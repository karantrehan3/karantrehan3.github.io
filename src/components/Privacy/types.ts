export interface PrivacySection {
  title: string;
  content: string;
  buttonIcon?: string;
  buttonText?: string;
  buttonMethod?: string;
  subtitle?: string;
}

export interface TableOfContentsItem {
  title: string;
  id: string;
}
